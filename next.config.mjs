/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  images: {
    domains: ['profile.line-scdn.net'],
  },
};

export default nextConfig;

version: '3.8'

services:
  web:
    container_name: massage-booking-app
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "3000:3000"
    # อ่านค่าจากไฟล์ .env (แนะนำวิธีนี้)
    env_file:
      - .env.local
    # หรือจะกำหนดค่าตรงๆ ก็ได้ (ไม่แนะนำถ้าเป็นความลับ)
    # environment:
    #   - NEXT_PUBLIC_SUPABASE_URL=...